---
phase: 09-port-collision-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/add-service/SKILL.md
autonomous: true
requirements:
  - PORT-03
  - PORT-04
  - PORT-05

must_haves:
  truths:
    - "Before Q2 is asked, all host-bound ports from installed services are collected into BOUND_PORTS"
    - "If .devtools/ does not exist, scan is silently skipped and Q2 is shown immediately"
    - "If the entered port conflicts, the user sees the conflicting service name and a suggested next-free port"
    - "A 3-option escape menu appears on each conflict: Try different port / Use internal only / Cancel"
    - "The re-prompt loop continues indefinitely while the user picks option 1"
    - "Choosing option 2 sets host_port=false and continues install with no port (same as Phase 8 opt-out)"
    - "Choosing option 3 exits cleanly with 'Cancelled. Nothing written.'"
    - "The guard applies to alias installs (same Step 5 path)"
  artifacts:
    - path: "skills/add-service/SKILL.md"
      provides: "Complete Step 5 with port scan sub-step + conflict-detection Q2"
      contains: "BOUND_PORTS"
  key_links:
    - from: "Step 5 Q1 Yes-branch"
      to: "Port scan sub-step"
      via: "Sequential flow — scan always runs before Q2 when host_port=true"
    - from: "Port scan sub-step"
      to: "Q2 conflict check"
      via: "BOUND_PORTS set populated by scan, consumed by Q2 validation"
    - from: "Q2 3-option menu option 2"
      to: "Phase 8 opt-out path"
      via: "Sets ANSWERS[host_port]=false — same downstream behavior as Q1 No"
---

<objective>
Insert the port collision guard into `skills/add-service/SKILL.md` Step 5 — the single
location where the Phase 8 host-port Q&A lives.

Purpose: Prevent two services from silently binding the same host port. Users see the
conflict immediately, know which service owns the port, and get a clear escape hatch.

Output: `skills/add-service/SKILL.md` with an expanded Step 5 that (1) scans all installed
service ports before asking Q2, and (2) replaces Q2's bare prompt with a conflict-checking
loop backed by a 3-option escape menu.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-port-collision-guard/09-CONTEXT.md
@.planning/phases/08-host-port-opt-in/08-CONTEXT.md

@skills/add-service/SKILL.md
</context>

<interfaces>
<!-- Current Step 5 Universal Q&A section (lines ~320-333 of SKILL.md) — the exact block
     being modified by this plan. Shown here so the executor knows the before-state. -->

```markdown
### Universal questions (all services):

1. **Expose this service on a host port? [y/N]** (default: N — internal Docker network only)
   - Store answer as `ANSWERS[host_port]=true/false`.
   - If **No (default)**: skip the Port question (question 2 below) entirely — proceed directly
     to question 3 (Image version). Do NOT ask for a port number. (D-04)
   - If **Yes**: continue to the Port question below. (D-06)

2. **Port?** `[default: <port from metadata>]` *(asked only if `ANSWERS[host_port]=true`)*
   - Store answer as `ANSWERS[port]`.

3. **Image version/tag?** `[default: <version from metadata>]`
   - Store answer as `ANSWERS[version]`.
```

<!-- KEY FACT: Q2's entire body ("Store answer as `ANSWERS[port]`.") must be REPLACED with
     the collision guard loop — not added-before or added-after, but replacing that single line. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert port scan sub-step (collect BOUND_PORTS before Q2)</name>
  <files>skills/add-service/SKILL.md</files>
  <action>
In `skills/add-service/SKILL.md`, locate the Step 5 Universal Q&A block. Insert a new
**Port Scan sub-step** immediately after the Q1 Yes-branch line
`"   - If **Yes**: continue to the Port question below. (D-06)"` and before Q2.

The inserted block must be indented to match Q1's body (3-space indent for content inside
the numbered list context), but the scan sub-step itself is a prose/instruction block, not
a numbered question. Insert it as a fenced instruction block between Q1 and Q2:

---

Replace this exact text in SKILL.md:

```
   - If **Yes**: continue to the Port question below. (D-06)

2. **Port?** `[default: <port from metadata>]` *(asked only if `ANSWERS[host_port]=true`)*
   - Store answer as `ANSWERS[port]`.
```

With this expanded version (per D-01, D-02, D-03, D-04, D-11, D-12, D-13):

```
   - If **Yes**: proceed to the Port Scan sub-step below, then ask Q2. (D-06, D-12)

   **Port Scan sub-step** *(runs before Q2, only when `ANSWERS[host_port]=true`)*

   Collect all host-bound ports from already-installed services into `BOUND_PORTS`
   (a mapping of port-number → service-slug). This scan happens once before Q2 is shown,
   avoiding repeated file I/O on each re-prompt cycle. (D-12)

   1. Check whether `.devtools/` exists:
      ```bash
      test -d .devtools
      ```
      - If `.devtools/` does **not** exist (clean project, first install): set
        `BOUND_PORTS={}` (empty) and skip straight to Q2. No user-visible message. (D-04)
      - If `.devtools/` exists: continue to scan steps 2–3 below.

   2. **Primary scan — compose files** (D-01, D-03): Read every compose file under
      `.devtools/` to extract host-side port numbers from `ports:` entries.

      ```bash
      find .devtools -name "*.compose.yml" -print0 | xargs -0 grep -h "127\.0\.0\.1:" 2>/dev/null
      ```

      For each matching line, the format is one of:
      - `"127.0.0.1:${ENV_VAR}:NNNN"` — extract the env-var's resolved value if available,
        or note the env var name for .env cross-reference.
      - `"127.0.0.1:NNNN:NNNN"` — extract the first NNNN as the host port.

      More precisely: from each port line, capture the token between `127.0.0.1:` and the
      second `:` (the container port). That token is either a literal port number or an
      `${ENV_VAR}` reference.

      To resolve `${ENV_VAR}` references, read the service's `.devtools/<slug>/.env` for
      the variable value. The service slug is the immediate parent directory of the compose
      file (e.g. `.devtools/redis/redis.compose.yml` → slug=`redis`).

      Add each resolved port number to `BOUND_PORTS` with the service slug as the value.
      If a port number appears more than once, keep the first occurrence. (D-07)

      **Scope:** Scan ALL containers in every compose file — main service, UI companion
      (redisinsight, pgadmin, phpmyadmin, mongo_express), and any monitoring exporter
      containers that have host-mapped ports. Do not filter by container type. (D-03)
      This also covers alias installs automatically, since their slugs appear as
      `.devtools/<service>-<alias>/` subdirectories. (D-13)

   3. **Supplemental scan — .env files** (D-02): Scan `.devtools/*/.env` for
      `*_PORT=<number>` variables to catch any port declared in env but not yet reflected
      in a compose file (edge case).

      ```bash
      find .devtools -maxdepth 2 -name ".env" -print0 | xargs -0 grep -hE "_PORT=[0-9]+" 2>/dev/null
      ```

      For each matched line `KEY=VALUE`, extract `VALUE` as a port number and the
      service slug from the parent directory. Add to `BOUND_PORTS` only if the port is
      not already present (compose scan is primary; .env is supplemental). (D-02)

   After steps 2–3, `BOUND_PORTS` contains every host-bound port number across all
   installed services. Proceed to Q2.

2. **Port?** `[default: <port from metadata>]` *(asked only if `ANSWERS[host_port]=true`)*
```

---

IMPORTANT formatting notes:
- Preserve the blank line between the Q1 block and the Port Scan sub-step header.
- Preserve the blank line between the Port Scan sub-step block and Q2.
- Q2's header line is unchanged; only its body (the line "   - Store answer as
  `ANSWERS[port]`.") will be replaced in Task 2.
- Do NOT alter the Q3 line, per-service credential sections, or anything below Step 5.
  </action>
  <verify>
    <automated>grep -n "BOUND_PORTS" skills/add-service/SKILL.md | head -20</automated>
  </verify>
  <done>
    `grep "BOUND_PORTS" skills/add-service/SKILL.md` returns at least 5 matches covering:
    the sub-step header, the empty-set silent-skip, the find+grep compose scan, and the
    find .env supplemental scan. Q2 header line is still present unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace Q2 body with conflict-detection loop + 3-option escape menu</name>
  <files>skills/add-service/SKILL.md</files>
  <action>
In `skills/add-service/SKILL.md`, locate Q2 (the Port question) in Step 5 Universal Q&A.
Its current body — after the Task 1 edit — is:

```
2. **Port?** `[default: <port from metadata>]` *(asked only if `ANSWERS[host_port]=true`)*
   - Store answer as `ANSWERS[port]`.
```

Replace the body line `"   - Store answer as \`ANSWERS[port]\`."` (the single bullet under
Q2) with the full collision-detection loop and 3-option escape menu (per D-05, D-06, D-07,
D-08, D-09, D-10):

---

Replace this exact text:

```
2. **Port?** `[default: <port from metadata>]` *(asked only if `ANSWERS[host_port]=true`)*
   - Store answer as `ANSWERS[port]`.
```

With this expanded version:

```
2. **Port?** `[default: <port from metadata>]` *(asked only if `ANSWERS[host_port]=true`)*

   Ask for the port number. After the user enters a value, run the conflict check:

   **Conflict check loop** (repeat after each "Try a different port" selection):

   - If the entered port is **not in** `BOUND_PORTS`: set `ANSWERS[port]=<value>` and
     proceed to Q3. ✓ No conflict.

   - If the entered port **is in** `BOUND_PORTS`: (D-05, D-06, D-07)
     - Identify the conflicting service: `CONFLICT_SERVICE = BOUND_PORTS[entered_port]`
       (if multiple services map to the same port, use the first one found). (D-07)
     - Find the next free port: scan `entered_port + 1`, `entered_port + 2`, … upward
       until a value not present in `BOUND_PORTS` is found. Call it `NEXT_FREE`. (D-06)
     - Display the conflict message:
       ```
       Port <entered_port> is already used by <CONFLICT_SERVICE> — try <NEXT_FREE>?
       ```
     - Show the 3-option escape menu and wait for user selection: (D-08)
       ```
       1. Try a different port
       2. Use internal only (no host binding)
       3. Cancel service install
       ```
     - **Option 1 — "Try a different port"**: Ask for a new port number (the conflict
       message above is a hint only — the user may enter any value, not just `NEXT_FREE`).
       (D-10) Re-run the conflict check with the new value. There is no retry limit —
       the loop continues as long as the user keeps selecting option 1. (D-09)
     - **Option 2 — "Use internal only (no host binding)"**: Set
       `ANSWERS[host_port]=false` and clear `ANSWERS[port]` (no port stored). Continue
       to Q3 (Image version). The install proceeds on the internal-only path — same
       downstream behaviour as a Q1 "No" answer (Phase 8 D-04). (D-08)
     - **Option 3 — "Cancel service install"**: Output `"Cancelled. Nothing written."`
       and stop. (D-08)
```

---

IMPORTANT formatting notes:
- The blank line after the Q2 header line is intentional — it separates the header from
  the prose introduction to the conflict check loop.
- Indentation uses 3 spaces for top-level bullets under Q2, matching the rest of Step 5.
- Do NOT alter Q3, the per-service credential sections, or any content below Step 5.
  </action>
  <verify>
    <automated>grep -n "Try a different port\|Use internal only\|Cancel service install\|already used by\|NEXT_FREE\|CONFLICT_SERVICE" skills/add-service/SKILL.md</automated>
  </verify>
  <done>
    All six grep terms return matches in SKILL.md:
    - "Try a different port" — option 1 label
    - "Use internal only (no host binding)" — option 2 label
    - "Cancel service install" — option 3 label
    - "already used by" — conflict message body
    - "NEXT_FREE" — next-free-port variable
    - "CONFLICT_SERVICE" — conflicting service variable

    The old single-bullet "Store answer as `ANSWERS[port]`." line is no longer present
    directly under Q2 (replaced by the loop prose).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI runtime → file system | The skill reads `.devtools/*` files; content is authored by prior skill runs, not untrusted user input |
| User input → port number | User types a port number at Q2; the skill compares it against BOUND_PORTS — no shell execution of the entered value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `.devtools/*/.env` scan | accept | Files are written by this skill only; an attacker who can write `.devtools/` already has project write access. Skill reads for port numbers only — no eval or execution of file content. |
| T-09-02 | Denial of Service | infinite re-prompt loop | accept | Loop is user-driven (option 1 re-prompt). User always has option 3 (Cancel) to exit. No automated loop possible without user interaction. |
| T-09-03 | Information Disclosure | conflict message names installed services | accept | Service names in `.devtools/` are not sensitive; the directory listing already exposes them to anyone with repo access. |
</threat_model>

<verification>
After both tasks are complete, verify the full collision guard integration in SKILL.md:

```bash
# 1. BOUND_PORTS scan sub-step is present and gated under host_port=true
grep -n "BOUND_PORTS\|Port Scan sub-step\|silent skip\|find .devtools" skills/add-service/SKILL.md

# 2. Conflict message format matches D-06 exactly
grep -n "already used by" skills/add-service/SKILL.md

# 3. Three escape options are present
grep -n "Try a different port\|Use internal only\|Cancel service install" skills/add-service/SKILL.md

# 4. Old bare "Store answer as ANSWERS[port]." line is gone
grep -n "Store answer as \`ANSWERS\[port\]\`" skills/add-service/SKILL.md
# Expected: 0 matches (replaced by the loop)

# 5. Q3 and per-service sections are untouched
grep -n "Image version/tag" skills/add-service/SKILL.md
# Expected: still present at its original relative position in Step 5
```
</verification>

<success_criteria>
1. `SKILL.md` Step 5 contains a "Port Scan sub-step" block between Q1-Yes and Q2, covering
   compose-file scanning (D-01, D-03) and .env supplemental scanning (D-02).
2. The scan block explicitly handles the `.devtools/` absent case with a silent skip (D-04).
3. Q2's body is the conflict-detection loop — no bare "Store answer as ANSWERS[port]." line
   remains under it.
4. The conflict message format matches D-06: `"Port N is already used by <service> — try M?"`.
5. The 3-option escape menu is present with all three options (D-08).
6. The loop notes no retry limit and that the suggested port is a hint only (D-09, D-10).
7. Option 2 notes it sets `ANSWERS[host_port]=false` (same as Phase 8 opt-out path).
8. Option 3 outputs `"Cancelled. Nothing written."` and stops.
9. All other sections of SKILL.md (Q3, per-service questions, Steps 6–13) are unmodified.
</success_criteria>

<output>
After completion, create `.planning/phases/09-port-collision-guard/09-01-SUMMARY.md`
following the standard summary template.
</output>
