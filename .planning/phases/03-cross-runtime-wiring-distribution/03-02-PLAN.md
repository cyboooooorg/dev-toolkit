---
phase: 03-cross-runtime-wiring-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-02

must_haves:
  truths:
    - "A developer reading README.md understands what this repo is in under 10 seconds"
    - "Install command `npx skills add Cyboooooorg/dev-tools` is clearly shown"
    - "All 5 supported services are listed with descriptions"
    - "What gets written to `.devtools/` is explained so users know what to expect"
    - "`.devtools/.env` gitignore note is present so users know credentials stay local"
  artifacts:
    - path: "README.md"
      provides: "Project documentation"
      contains: "npx skills add Cyboooooorg/dev-tools"
  key_links:
    - from: "README.md"
      to: "skills/add-service/SKILL.md"
      via: "documents the skill's capabilities"
      pattern: "npx skills add"
---

<objective>
Write `README.md` at the repository root — replacing the existing placeholder — so any developer
can install and use the `add-service` skill in one read.

Purpose: Satisfies DIST-02. The README is the primary user-facing document for this repo;
it must enable a developer to go from "never heard of this" to "skill installed and first
service added" in a single session.

Output: A complete, markdownlint-compliant `README.md` covering what this repo is, how to
install the skill, how to invoke it, all 5 supported services, what gets written, and the
`.env` gitignore note (per locked decision D-05).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cross-runtime-wiring-distribution/03-CONTEXT.md
@.planning/phases/03-cross-runtime-wiring-distribution/03-01-SUMMARY.md

<interfaces>
<!-- Content requirements from locked decision D-05 -->

README MUST cover (D-05):
  1. What this repo is — a dev-tools skill collection (1-2 sentences)
  2. Install command: npx skills add Cyboooooorg/dev-tools
  3. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB
  4. Quick usage: /add-service redis  OR  /add-service (natural language trigger)
  5. What gets written to .devtools/: <service>.compose.yml, <service>.Taskfile.yml, .env entries
  6. Note: .devtools/.env is gitignored — credentials stay local

README MUST NOT (from RESEARCH.md anti-patterns):
  - List "monitoring" as a user-facing service (it's an internal template, not in SKILL-03's 5)
  - Describe manual symlinking to .github/skills/ or .agents/skills/ (superseded by D-04)
  - Use bare URLs (markdownlint compliance)

skills.sh install facts (from 03-RESEARCH.md):
  - `npx skills add Cyboooooorg/dev-tools` installs to all detected agents in the project
  - GitHub Copilot path: .agents/skills/add-service/
  - Claude Code path: .claude/skills/add-service/
  - Covers 40+ agents total (Cursor, Codex, Cline, Gemini CLI, etc.)
  - Reference: https://agentskills.io for full agent list

SKILL.md capability summary (from skills/add-service/SKILL.md):
  - Trigger: natural language "add Redis to this project" OR slash command /add-service
  - Asks: port, image version, credentials before writing anything
  - Shows confirmation gate before any file is created
  - Merge-safe: detects existing installs, supports named second instances (aliases)
  - Idempotent: re-running for same alias is a no-op

Output files (per PROJECT.md and SKILL.md):
  .devtools/<service>.compose.yml     — Docker Compose service fragment
  .devtools/<service>.Taskfile.yml    — per-service Taskfile (up/down/logs/restart)
  .devtools/.env                      — credentials (gitignored, appended not overwritten)
  Root Taskfile.yml includes entry    — added to project root Taskfile if present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
    Replace the existing `README.md` with the complete content below. Write it verbatim —
    do not summarise or paraphrase. The structure follows the pattern from
    `github.com/vercel-labs/agent-skills` adapted to D-05 requirements.

    Markdownlint rules to follow:
    - ATX headings only (# not underline style)
    - No bare URLs — all links use `[text](url)` format
    - Blank line before and after code blocks
    - Blank line before and after headings
    - No trailing spaces
    - Single blank line between sections (no double blank lines)

    ---

    ```markdown
    # dev-tools

    > AI skills for adding Docker dev services to any project.

    One command adds a fully configured Docker Compose service and Taskfile to your project.
    Supports Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, and MongoDB.

    ## Installation

    Run this once in your project root:

    ```bash
    npx skills add Cyboooooorg/dev-tools
    ```

    This installs the skill to all detected AI agents in your project
    (GitHub Copilot, Claude Code, Cursor, and [more](https://agentskills.io)).

    ## Usage

    Trigger the skill with natural language or a slash command:

    ```
    add Redis to this project
    ```

    ```
    /add-service postgres
    ```

    The skill will ask for port, image version, and credentials before writing anything.
    A confirmation summary is shown before any file is created.

    ## Supported Services

    | Service | Description |
    |---------|-------------|
    | **Redis** | In-memory key-value store. Ideal for caching and session storage. |
    | **RabbitMQ** | Message broker with optional Management UI (port 15672). |
    | **PostgreSQL** | Relational database with named volume and health check. |
    | **MySQL / MariaDB** | MySQL-compatible database (defaults to MariaDB on ARM64). |
    | **MongoDB** | Document database with named volume and health check. |

    ## What Gets Written

    Files are created in a `.devtools/` directory in your project root:

    ```
    .devtools/
      <service>.compose.yml    # Docker Compose service fragment
      <service>.Taskfile.yml   # Tasks: up, down, logs, restart
      .env                     # Credentials (gitignored)
    ```

    Your root `Taskfile.yml` (if present) gets an `includes:` entry pointing to the new
    per-service Taskfile. Each run appends to `.devtools/.env` — existing services are
    never overwritten.

    > **Note:** `.devtools/.env` is gitignored. Credentials stay on your machine.

    ## Multiple Instances

    Running the skill for a service that already exists prompts you to add a named second
    instance (e.g., `redis-cache`, `redis-session`). All files and env vars are namespaced
    by alias. Re-running with the same alias is a no-op — safe to invoke twice.
    ```

    After writing the file, verify it with:

    ```bash
    cat README.md
    ```

    Confirm the install command `npx skills add Cyboooooorg/dev-tools` appears exactly once,
    all 5 services are listed in the table, and no mention of `monitoring` service or manual
    symlinks appears anywhere in the file.
  </action>
  <verify>
    Run all three checks:

    1. `grep -c "npx skills add Cyboooooorg/dev-tools" README.md` → must return `1`
    2. `grep -E "Redis|RabbitMQ|PostgreSQL|MySQL|MongoDB" README.md | wc -l` → must return ≥ 5
    3. `grep -i "monitoring\|\.github/skills\|\.agents/skills" README.md` → must return empty (no output)
  </verify>
  <done>
    `README.md` exists at repo root with all D-05 content requirements satisfied:
    install command present, all 5 services listed, `.devtools/` output explained,
    `.env` gitignore note included, no mention of monitoring or manual symlinks.
    File is markdownlint-compliant.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → public internet | README is public-facing; must not leak internal paths, credentials, or implementation details beyond what is user-relevant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | README.md content | accept | README describes public capability only. No secrets, no internal paths beyond `.devtools/` which is the documented output. |
| T-03-02-02 | Tampering | README.md in git | accept | README is documentation; changes are visible in git diff. No runtime impact. |
</threat_model>

<verification>
After task completion, three checks must all pass:

```bash
grep -c "npx skills add Cyboooooorg/dev-tools" README.md   # → 1
grep -E "Redis|RabbitMQ|PostgreSQL|MySQL|MongoDB" README.md | wc -l   # → ≥ 5
grep -i "monitoring\|\.github/skills\|\.agents/skills" README.md   # → empty
```
</verification>

<success_criteria>
- `README.md` exists at repo root
- Install command `npx skills add Cyboooooorg/dev-tools` is present exactly once
- All 5 services (Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB) listed in a table
- `.devtools/` output structure described (`.compose.yml`, `.Taskfile.yml`, `.env`)
- `.devtools/.env` gitignore note is present
- No mention of `monitoring` service, manual symlinks, or `.github/skills/` paths
- DIST-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-runtime-wiring-distribution/03-02-SUMMARY.md`

Include:
- What was written (README.md section summary)
- Verification results (grep outputs)
- Confirmation DIST-02 is satisfied
- Note if any D-05 content item was omitted and why (should be none)
</output>
