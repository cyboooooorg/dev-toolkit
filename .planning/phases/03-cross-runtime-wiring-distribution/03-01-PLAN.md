---
phase: 03-cross-runtime-wiring-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/add-service/SKILL.md
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-03
  - DIST-01

must_haves:
  truths:
    - "Running `npx skills add Cyboooooorg/dev-tools` finds and installs the add-service skill"
    - "The skill frontmatter has `name: add-service` and a quoted `description` string that enumerates all 5 services"
    - "The repo directory structure matches what skills.sh CLI expects: `skills/add-service/SKILL.md`"
  artifacts:
    - path: "skills/add-service/SKILL.md"
      provides: "Canonical skill definition with valid frontmatter"
      contains: "name: add-service"
  key_links:
    - from: "skills/add-service/SKILL.md"
      to: "skills.sh CLI"
      via: "npx skills add Cyboooooorg/dev-tools"
      pattern: "name:.*add-service"
---

<objective>
Verify that `skills/add-service/SKILL.md` is fully valid for skills.sh discovery and that
`npx skills add Cyboooooorg/dev-tools` would install the skill to both `.agents/skills/add-service/`
(GitHub Copilot / most agents) and `.claude/skills/add-service/` (Claude Code) in a consumer project.

Purpose: Gate the README-writing plan on confirmed discoverability. If a frontmatter issue is
found, fix it here before documenting the install flow.

Output: A verified (and if needed, corrected) `skills/add-service/SKILL.md` with valid frontmatter.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cross-runtime-wiring-distribution/03-CONTEXT.md
@.planning/phases/03-cross-runtime-wiring-distribution/03-RESEARCH.md

<interfaces>
<!-- skills.sh frontmatter requirements (from RESEARCH.md, verified against vercel-labs/skills source) -->

Required SKILL.md frontmatter fields (both must be strings):
  name: <string>          # Becomes the skill directory name on install
  description: <string>   # Must be YAML-safe; quote if it contains colons

Optional fields passed through to agents:
  argument-hint: "<service-name>"
  allowed-tools: Read, Write, Bash, AskUserQuestion

Discovery path the CLI expects: skills/<name>/SKILL.md
Install paths written by `npx skills add`:
  - .agents/skills/<name>/   (GitHub Copilot, Cursor, Codex, Gemini CLI, Cline, 40+ others)
  - .claude/skills/<name>/   (Claude Code)

Current SKILL.md frontmatter (from commit 597bd26):
  name: add-service
  description: "Add a Docker dev service to this project. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB. Writes Docker Compose and Taskfile configs to .devtools/."
  argument-hint: "<service-name>"
  allowed-tools: Read, Write, Bash, AskUserQuestion
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SKILL.md frontmatter and fix any issues</name>
  <files>skills/add-service/SKILL.md</files>
  <action>
    Read `skills/add-service/SKILL.md` and confirm the following (per D-02, D-03, D-07):

    1. **File path check** — Confirm the file is at exactly `skills/add-service/SKILL.md`.
       This is the path the skills.sh CLI discovers (`skills/<name>/SKILL.md` convention).

    2. **Frontmatter `name` field** — Must be the unquoted string `add-service`. This value
       becomes the directory name on install (`.agents/skills/add-service/`). No version
       suffixes, no underscores.

    3. **Frontmatter `description` field** — Must be a quoted YAML string (colons present, so
       quotes are required to prevent YAML parse failure). Must explicitly enumerate all 5
       supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB. The current
       value is: `"Add a Docker dev service to this project. Supported services: Redis,
       RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB. Writes Docker Compose and Taskfile
       configs to .devtools/."` — verify this is unchanged and correctly quoted.

    4. **No extra required fields** — skills.sh only requires `name` and `description`.
       `argument-hint` and `allowed-tools` are optional/pass-through; their presence is fine.

    5. **DIST-01 confirmation note** — Add a brief comment in the SUMMARY explaining why no
       manual `.github/skills/` or `.agents/skills/` wiring is done in this repo: skills.sh
       handles multi-agent path installation at consumer install time (D-04 / D-06).

    If all checks pass with no changes needed, still commit the verification as a no-op
    with message `chore(03-01): verify SKILL.md frontmatter valid for skills.sh discovery`.

    If any fix is required (e.g., unquoted description, wrong name value), apply the minimal
    correction and commit with `fix(03-01): correct SKILL.md frontmatter for skills.sh`.

    Do NOT modify any content below the frontmatter block. The skill body is Phase 2 work —
    it is out of scope here.
  </action>
  <verify>
    Run: `head -6 skills/add-service/SKILL.md`

    Expected output:
    ```
    ---
    name: add-service
    description: "Add a Docker dev service to this project. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB. Writes Docker Compose and Taskfile configs to .devtools/."
    argument-hint: "<service-name>"
    allowed-tools: Read, Write, Bash, AskUserQuestion
    ---
    ```

    Also run: `ls skills/add-service/` → must show `SKILL.md`

    Optional (if npx available): `npx --yes skills add --list .` → must list `add-service`
    as a discoverable skill from this repo.
  </verify>
  <done>
    `skills/add-service/SKILL.md` exists at the correct path, has `name: add-service` and
    a quoted `description` string naming all 5 services. Verification committed to git.
    DIST-01 (skills.sh discoverability) confirmed. SKILL-01, SKILL-02, SKILL-03 confirmed
    satisfied by the current implementation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This repo → skills.sh CLI | CLI fetches and parses SKILL.md; malformed frontmatter causes silent skip |
| SKILL.md → consumer AI agent | Agent executes skill instructions; skill body is sandboxed by agent runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | skills/add-service/SKILL.md frontmatter | accept | YAML is static, committed to git. No runtime execution of frontmatter. |
| T-03-01-02 | Repudiation | git commit of verification result | accept | Git history provides audit trail of what was verified and when. |
| T-03-01-03 | Information Disclosure | description field reveals supported services | accept | By design — disclosure of capability list is the goal. No secrets in frontmatter. |
</threat_model>

<verification>
After task completion:
- `head -6 skills/add-service/SKILL.md` shows correct frontmatter
- `ls skills/add-service/` shows `SKILL.md` exists
- Git log shows a verification or fix commit for plan 03-01
</verification>

<success_criteria>
- `skills/add-service/SKILL.md` frontmatter is valid per skills.sh requirements
- `name` is `add-service` (string, no quotes needed)
- `description` is a quoted string enumerating all 5 services (Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB)
- No manual `.github/skills/` or `.agents/skills/` symlinks created in this repo (per D-04)
- SKILL-01, SKILL-02, SKILL-03, DIST-01 are all confirmed satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-cross-runtime-wiring-distribution/03-01-SUMMARY.md`

Include:
- What was verified (frontmatter fields checked, path confirmed)
- Whether any fix was applied (or "no changes needed")
- Confirmation that DIST-01 is satisfied via skills.sh (not manual symlinks)
- Note: D-04 / D-06 explain why `.github/skills/` and `.agents/skills/` are NOT wired in this repo
</output>
