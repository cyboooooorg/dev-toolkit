---
phase: 04-subfolder-output-structure-per-service-subdirectory-layout-i
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - skills/add-service/SKILL.md
  - taskfile-templates/root/Taskfile.yml
autonomous: true
requirements:
  - CRED-01
  - CRED-02
  - CRED-03
  - TMPL-08

must_haves:
  truths:
    - "Step 10a writes service credentials to .devtools/${SERVICE_SLUG}/.env (not root .devtools/.env)"
    - "Root .devtools/.env receives only COMPOSE_PROJECT_NAME and COMPOSE_PROFILES (first install only)"
    - "Step 10d (monitoring) writes to .devtools/monitoring/monitoring.compose.yml and .devtools/monitoring/.env"
    - "Step 11a include paths use redis/redis.compose.yml (subfolder-relative, no leading ./)"
    - "Step 11b commentary references new subfolder paths"
    - "taskfile-templates/root/Taskfile.yml uses redis/redis.Taskfile.yml (not ./redis.Taskfile.yml)"
  artifacts:
    - path: "skills/add-service/SKILL.md"
      provides: "Updated .env management (Step 10) and root include paths (Step 11)"
      contains: ".devtools/${SERVICE_SLUG}/.env"
    - path: "taskfile-templates/root/Taskfile.yml"
      provides: "Root Taskfile template with subfolder-relative include paths"
      contains: "taskfile: redis/redis.Taskfile.yml"
  key_links:
    - from: "Step 10a per-service .env write"
      to: ".devtools/${SERVICE_SLUG}/.env"
      via: "mkdir created in Step 9a"
      pattern: "\\.devtools/\\$\\{SERVICE_SLUG\\}/\\.env"
    - from: "Step 11a compose include"
      to: ".devtools/redis/redis.compose.yml"
      via: "subfolder-relative path in include: block"
      pattern: "redis/redis\\.compose\\.yml"
---

<objective>
Update SKILL.md Step 10 (.env management) and Step 11 (root file management), and update
the root Taskfile template file to use subfolder paths.

Purpose: The .env split is the core behavioral change of Phase 4 — service credentials now
go to `.devtools/${SERVICE_SLUG}/.env` instead of the shared root `.devtools/.env`. Root
`.devtools/.env` becomes minimal (COMPOSE_PROJECT_NAME + COMPOSE_PROFILES only). Root
compose.yml and Taskfile.yml include paths must use `redis/redis.compose.yml` format.

Output:
- `skills/add-service/SKILL.md`: Step 10 rewrites service vars to subfolder `.env`;
  root `.env` only gets COMPOSE_PROJECT_NAME + COMPOSE_PROFILES on first install.
  Step 10d monitoring files go to `.devtools/monitoring/` subfolder.
  Step 11a include paths: `redis/redis.compose.yml` (D-09).
  Step 11b commentary updated for new paths (D-10, D-08).
- `taskfile-templates/root/Taskfile.yml`: All 6 includes updated to subfolder paths
  (e.g. `redis/redis.Taskfile.yml` instead of `./redis.Taskfile.yml`) (D-10).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-CONTEXT.md
@skills/add-service/SKILL.md
@taskfile-templates/root/Taskfile.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Step 10 .env management — split root vs per-service subfolder</name>
  <files>skills/add-service/SKILL.md</files>
  <read_first>
    - skills/add-service/SKILL.md — read the complete Step 10 block (Steps 10a through 10d)
      before making any edits; the section starts at "## Step 10: .env Management"
    - .planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-CONTEXT.md
      — D-05 (per-service .env in subfolder), D-06 (root .env minimal), D-07 (no template
      changes needed for env_file resolution), D-08 (root files stay at .devtools/ root)
  </read_first>
  <action>
Apply the following 3 precise find-and-replace edits to `skills/add-service/SKILL.md`.
These edits restructure Step 10 to split .env writes between the per-service subfolder
and the minimal root .devtools/.env.

---

**Edit 1 — Step 10 header and Step 10a: per-service .env write (D-05, D-06):**

Find EXACTLY (the complete Step 10a block, from "## Step 10:" through the end of 10a):
```
## Step 10: .env Management

### 10a: Write .devtools/.env

Append a section block to `.devtools/.env` (create the file if it does not exist):

```
# ── <SERVICE> ───────────────────────────────────────
<ENV_VAR_NAME>=<value>
...
```

For **standard** install — env var names from metadata `env_var` field:

| Service | Env vars to append |
|---------|-------------------|
| redis | REDIS_PORT, REDIS_VERSION, REDIS_PASSWORD (empty string if skipped) |
| rabbitmq | RABBITMQ_PORT, RABBITMQ_VERSION, RABBITMQ_USERNAME, RABBITMQ_PASSWORD, RABBITMQ_UI_PORT |
| postgres | POSTGRES_PORT, POSTGRES_VERSION, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_UI_PORT (if UI enabled), PGADMIN_DEFAULT_EMAIL (if UI enabled), PGADMIN_DEFAULT_PASSWORD (if UI enabled) |
| mysql | MYSQL_PORT, MYSQL_VERSION, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE, MYSQL_ROOT_PASSWORD, MYSQL_UI_PORT (if UI enabled) |
| mongodb | MONGODB_PORT, MONGODB_VERSION, MONGO_INITDB_ROOT_USERNAME, MONGO_INITDB_ROOT_PASSWORD, MONGODB_UI_PORT (if UI enabled), MONGO_EXPRESS_BASICAUTH (if UI enabled: `true` if auth, `false` if not), MONGO_EXPRESS_USER (if UI auth), MONGO_EXPRESS_PASSWORD (if UI auth) |

For **alias** install — prefix all env var names with `ENV_PREFIX_`:
e.g. `REDIS_CACHE_PORT`, `REDIS_CACHE_VERSION`, `REDIS_CACHE_PASSWORD`, etc.

**Conflict detection (D-13):** Before appending each key, check if the key already exists
in `.devtools/.env`. If it does:
- Mark the old line: append ` ## REPLACED` as an inline comment on that line.
- Append the new value on the next line.
- Record the conflict in a warnings list for the done summary.

If `COMPOSE_PROJECT_NAME` is not yet in `.devtools/.env`, prepend it as the very first entry.
Immediately after `COMPOSE_PROJECT_NAME`, if `COMPOSE_PROFILES` is not yet in `.devtools/.env`,
add it on the next line:

```
COMPOSE_PROJECT_NAME=<value>
COMPOSE_PROFILES=services
```

`COMPOSE_PROFILES=services` ensures `docker compose up` (with no `--profile` flag) starts
the core service containers. Without it the default profile is empty and all containers are
skipped. Never overwrite an existing `COMPOSE_PROFILES` entry — the user may have added `ui`
or `monitoring` to it.
```

Replace with:
```
## Step 10: .env Management

### 10a: Write per-service .devtools/${SERVICE_SLUG}/.env  (D-05)

The service subdirectory was created by `mkdir -p` in Step 9a. Write service credentials
and config to `.devtools/${SERVICE_SLUG}/.env` (create if not exists, append if exists):

```
# ── <SERVICE> ───────────────────────────────────────
<ENV_VAR_NAME>=<value>
...
```

For **standard** install — env var names from metadata `env_var` field:

| Service | Env vars to write |
|---------|------------------|
| redis | REDIS_PORT, REDIS_VERSION, REDIS_PASSWORD (empty string if skipped) |
| rabbitmq | RABBITMQ_PORT, RABBITMQ_VERSION, RABBITMQ_USERNAME, RABBITMQ_PASSWORD, RABBITMQ_UI_PORT |
| postgres | POSTGRES_PORT, POSTGRES_VERSION, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_UI_PORT (if UI enabled), PGADMIN_DEFAULT_EMAIL (if UI enabled), PGADMIN_DEFAULT_PASSWORD (if UI enabled) |
| mysql | MYSQL_PORT, MYSQL_VERSION, MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE, MYSQL_ROOT_PASSWORD, MYSQL_UI_PORT (if UI enabled) |
| mongodb | MONGODB_PORT, MONGODB_VERSION, MONGO_INITDB_ROOT_USERNAME, MONGO_INITDB_ROOT_PASSWORD, MONGODB_UI_PORT (if UI enabled), MONGO_EXPRESS_BASICAUTH (if UI enabled: `true` if auth, `false` if not), MONGO_EXPRESS_USER (if UI auth), MONGO_EXPRESS_PASSWORD (if UI auth) |

For **alias** install — prefix all env var names with `ENV_PREFIX_`:
e.g. `REDIS_CACHE_PORT`, `REDIS_CACHE_VERSION`, `REDIS_CACHE_PASSWORD`, etc.

**Conflict detection:** Before appending each key, check if the key already exists in
`.devtools/${SERVICE_SLUG}/.env`. If it does:
- Mark the old line: append ` ## REPLACED` as an inline comment on that line.
- Append the new value on the next line.
- Record the conflict in a warnings list for the done summary.

**Why per-service subfolder, not root .devtools/.env?** (D-05/D-06)
Credentials belong to their service. Deleting `.devtools/redis/` removes Redis completely —
including its credentials — without touching other services. The root `.devtools/.env` stays
minimal (project-level vars only), making it safe to commit (after removing any leaked values).

### 10a-root: Write root .devtools/.env (project-level vars only)  (D-06)

The root `.devtools/.env` holds ONLY project-level vars that Docker Compose needs when
running the root `compose.yml`. Write these on first install only (skip if already present):

```
COMPOSE_PROJECT_NAME=<value>
COMPOSE_PROFILES=services
```

- If `COMPOSE_PROJECT_NAME` is not yet in `.devtools/.env`, prepend it as the very first entry.
- Immediately after, if `COMPOSE_PROFILES` is not yet in `.devtools/.env`, add it.
- `COMPOSE_PROFILES=services` ensures `docker compose up` starts core service containers.
  Without it the default profile is empty and all containers are skipped.
- Never overwrite an existing `COMPOSE_PROFILES` entry — the user may have added `ui`
  or `monitoring` to it.
- Do NOT write any service credentials or ports to root `.devtools/.env`.
```

---

**Edit 2 — Step 10b: update .env.example (D-06, D-08):**

Find EXACTLY (the complete Step 10b block):
```
### 10b: Write .devtools/.env.example

Append the same keys to `.devtools/.env.example` (create if not exists) with **dummy/placeholder
values only** — never real credentials:

```
# ── <SERVICE> ───────────────────────────────────────
REDIS_PORT=6379
REDIS_VERSION=7
REDIS_PASSWORD=CHANGE_ME
REDIS_UI_PORT=5540
```

On the first service install, also add to `.devtools/.env.example`:
```
COMPOSE_PROJECT_NAME=myapp
COMPOSE_PROFILES=services
```

Use obvious placeholder strings for credentials: `CHANGE_ME`, `admin@example.com`, etc.
```

Replace with:
```
### 10b: Write .devtools/.env.example  (D-06, D-08)

The root `.devtools/.env.example` documents the project-level structure only. Update it on
first install (skip if `COMPOSE_PROJECT_NAME` is already present):

```
# .devtools/.env — project-level vars (safe to inspect; do NOT commit per-service .env files)
COMPOSE_PROJECT_NAME=myapp
COMPOSE_PROFILES=services

# Per-service credentials live in their service subfolders:
#   .devtools/redis/.env       — Redis credentials
#   .devtools/postgres/.env    — PostgreSQL credentials
#   .devtools/rabbitmq/.env    — RabbitMQ credentials
#   .devtools/mysql/.env       — MySQL credentials
#   .devtools/mongodb/.env     — MongoDB credentials
# All subfolder .env files are gitignored via **/.env in .devtools/.gitignore
```

Do NOT append per-service credential keys to `.devtools/.env.example`. Per-service vars are
self-documenting in `.devtools/${SERVICE_SLUG}/.env` which the skill creates.
```

---

**Edit 3 — Step 10c and 10d: monitoring env and files go to subfolder (D-01–D-04):**

Find EXACTLY (the complete Step 10c block):
```
### 10c: Monitoring .env (if ANSWERS[monitoring]=true)

Append to `.devtools/.env`:
```
# ── Monitoring ──────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=<ANSWERS[grafana_password]>
```

Apply conflict detection (D-13) for `GRAFANA_ADMIN_USER` and `GRAFANA_ADMIN_PASSWORD`.

Append to `.devtools/.env.example` (dummy values only):
```
# ── Monitoring ──────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME
```
```

Replace with:
```
### 10c: Monitoring .env (if ANSWERS[monitoring]=true)

Write monitoring credentials to `.devtools/monitoring/.env`
(the `mkdir -p .devtools/monitoring` will be run in Step 10d before writing these):

```
# ── Monitoring ──────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=<ANSWERS[grafana_password]>
```

Apply conflict detection for `GRAFANA_ADMIN_USER` and `GRAFANA_ADMIN_PASSWORD` within
`.devtools/monitoring/.env`. Do NOT write monitoring vars to root `.devtools/.env`.
```

Then find EXACTLY (the complete Step 10d block):
```
### 10d: Monitoring files (if ANSWERS[monitoring]=true)

Copy monitoring templates verbatim (no token substitution needed):

1. Fetch and write the compose template:
   ```bash
   curl -fsSL "${SKILL_RAW_BASE}/compose-templates/monitoring/monitoring.compose.yml"
   ```
   Write fetched content to `.devtools/monitoring.compose.yml` in the target project.

2. If `ANSWERS[taskfile]=true`, fetch and write the Taskfile:
   ```bash
   curl -fsSL "${SKILL_RAW_BASE}/taskfile-templates/monitoring/monitoring.Taskfile.yml"
   ```
   Write fetched content to `.devtools/monitoring.Taskfile.yml` in the target project.

3. After writing `monitoring.compose.yml` (step 1 above), update `.devtools/compose.yml`
   to add the monitoring include (using the same create-or-append logic as Step 11a):
   ```yaml
     - ./monitoring.compose.yml
   ```

**Important:** Write `monitoring.compose.yml` before updating `compose.yml` include —
same write-order rule as Step 9 (Pitfall 6).
```

Replace with:
```
### 10d: Monitoring files (if ANSWERS[monitoring]=true)

Copy monitoring templates verbatim (no token substitution needed):

1. Create monitoring subfolder and write the compose template:
   ```bash
   mkdir -p .devtools/monitoring
   curl -fsSL "${SKILL_RAW_BASE}/compose-templates/monitoring/monitoring.compose.yml"
   ```
   Write fetched content to `.devtools/monitoring/monitoring.compose.yml`.
   Then write `.devtools/monitoring/.env` as specified in Step 10c.

2. If `ANSWERS[taskfile]=true`, fetch and write the Taskfile:
   ```bash
   curl -fsSL "${SKILL_RAW_BASE}/taskfile-templates/monitoring/monitoring.Taskfile.yml"
   ```
   Write fetched content to `.devtools/monitoring/monitoring.Taskfile.yml`.

3. After writing `monitoring/monitoring.compose.yml` (step 1 above), update `.devtools/compose.yml`
   to add the monitoring include (using the same create-or-append logic as Step 11a):
   ```yaml
     - monitoring/monitoring.compose.yml
   ```

**Important:** Write `monitoring/monitoring.compose.yml` before updating `compose.yml` include —
same write-order rule as Step 9.
```

---
  </action>
  <verify>
    <automated>
grep -n 'SERVICE_SLUG}/.env' skills/add-service/SKILL.md | head -10
grep -n '10a-root' skills/add-service/SKILL.md
grep -n 'monitoring/monitoring.compose.yml' skills/add-service/SKILL.md
grep -n 'mkdir -p .devtools/monitoring' skills/add-service/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep '\.devtools/\${SERVICE_SLUG}/\.env' skills/add-service/SKILL.md` returns at least
      2 matches (Step 10a header and conflict detection reference)
    - `grep '10a-root' skills/add-service/SKILL.md` returns exactly 1 match (the new sub-step)
    - `grep 'COMPOSE_PROJECT_NAME.*COMPOSE_PROFILES' skills/add-service/SKILL.md` still returns
      matches (project-level vars still documented, just in 10a-root now)
    - `grep 'monitoring/monitoring\.compose\.yml' skills/add-service/SKILL.md` returns at least
      2 matches (Step 10d write + Step 10d include update)
    - `grep 'mkdir -p .devtools/monitoring' skills/add-service/SKILL.md` returns exactly 1 match
    - `grep '\.devtools/monitoring\.compose\.yml' skills/add-service/SKILL.md` returns 0 matches
      (old flat monitoring path is gone)
    - The old "Append to `.devtools/.env`" heading with monitoring vars does NOT appear;
      `grep 'Append to.*\.devtools/\.env' skills/add-service/SKILL.md` returns 0 matches
  </acceptance_criteria>
  <done>
    Step 10a writes service credentials to per-service subfolder .env.
    New Step 10a-root writes only COMPOSE_PROJECT_NAME/COMPOSE_PROFILES to root .devtools/.env.
    Step 10b .env.example documents project-level structure with subfolder explanation.
    Step 10c/10d monitoring vars and files go to .devtools/monitoring/ subfolder.
    No old flat paths (.devtools/monitoring.compose.yml) remain in Step 10.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Step 11 root include paths and update root Taskfile template</name>
  <files>skills/add-service/SKILL.md, taskfile-templates/root/Taskfile.yml</files>
  <read_first>
    - skills/add-service/SKILL.md — read the complete Step 11 block (Steps 11a and 11b)
      before editing; the section starts at "## Step 11: Root File Management"
    - taskfile-templates/root/Taskfile.yml — read completely; it has an includes: block with
      6 services using `./redis.Taskfile.yml` style paths that all need updating
    - .planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-CONTEXT.md
      — D-08 (root files stay at .devtools/ root), D-09 (compose include paths), D-10
      (Taskfile include paths + update template)
  </read_first>
  <action>
Apply edits to TWO files: `skills/add-service/SKILL.md` (Steps 11a and 11b) and
`taskfile-templates/root/Taskfile.yml` (6 include path updates).

---

**SKILL.md Edit 1 — Step 11a: root compose.yml include paths (D-09):**

Find EXACTLY (the Step 11a first-install block):
```
**If it does NOT exist (first service install):**
Create `.devtools/compose.yml` with this content:
```yaml
# .devtools/compose.yml
# Root compose aggregator — managed by dev-tools skill
# Do not edit manually; run the skill to add or remove services.

include:
  - ./<filename>.compose.yml
```
Where `<filename>` is `${SERVICE}` (standard) or `${SERVICE_SLUG}` (alias).
```

Replace with:
```
**If it does NOT exist (first service install):**
Create `.devtools/compose.yml` with this content:
```yaml
# .devtools/compose.yml
# Root compose aggregator — managed by dev-tools skill
# Do not edit manually; run the skill to add or remove services.

include:
  - path: <slug>/<filename>.compose.yml
```
Where `<slug>/<filename>` is `redis/redis` (standard) or `redis-cache/redis-cache` (alias).
The `path:` key is used for explicitness; bare list entries are also valid Docker Compose v2.
(D-09)
```

Then find EXACTLY (the Step 11a subsequent-install block):
```
**If it DOES exist (subsequent service install):**
Read the file, locate the `include:` block, and append a new line:
```yaml
  - ./<filename>.compose.yml
```
Do NOT rewrite the entire file — only add the new entry.
```

Replace with:
```
**If it DOES exist (subsequent service install):**
Read the file, locate the `include:` block, and append a new line:
```yaml
  - path: <slug>/<filename>.compose.yml
```
Where `<slug>/<filename>` is `redis/redis` (standard) or `redis-cache/redis-cache` (alias).
Do NOT rewrite the entire file — only add the new entry. (D-09)
```

---

**SKILL.md Edit 2 — Step 11b: root Taskfile.yml commentary (D-10, D-08):**

Find EXACTLY (the Step 11b "If it DOES exist" block and the important note):
```
**If it DOES exist:** Do NOT overwrite it. The root Taskfile template already contains
`optional: true` for all 6 service includes — missing service files are silently skipped
at runtime. No append is needed.

**Important:** Never overwrite an existing `.devtools/Taskfile.yml`. Users may have
customized it. The `optional: true` pattern makes per-install updates unnecessary.
```

Replace with:
```
**If it DOES exist:** Do NOT overwrite it. The root Taskfile template contains
`optional: true` for all 6 service includes using subfolder-relative paths (e.g.
`redis/redis.Taskfile.yml`) — missing service files are silently skipped at runtime.
No append is needed for the Taskfile. (D-10)

**Important:** Never overwrite an existing `.devtools/Taskfile.yml`. Users may have
customized it. The `optional: true` pattern makes per-install updates unnecessary.

**Root file locations (D-08):** `.devtools/compose.yml`, `.devtools/Taskfile.yml`,
`.devtools/.gitignore`, and `.devtools/.env` all remain at the `.devtools/` root —
their locations do not change with the subfolder layout. Only per-service files
move into subfolders.
```

---

**taskfile-templates/root/Taskfile.yml Edit — update all 6 include paths (D-10):**

Find EXACTLY (the complete includes: block in taskfile-templates/root/Taskfile.yml):
```
includes:
  redis:
    taskfile: ./redis.Taskfile.yml
    optional: true # Silently ignored if Redis is not installed
  rabbitmq:
    taskfile: ./rabbitmq.Taskfile.yml
    optional: true
  postgres:
    taskfile: ./postgres.Taskfile.yml
    optional: true
  mysql:
    taskfile: ./mysql.Taskfile.yml
    optional: true
  mongodb:
    taskfile: ./mongodb.Taskfile.yml
    optional: true
  monitoring:
    taskfile: ./monitoring.Taskfile.yml
    optional: true
```

Replace with:
```
includes:
  redis:
    taskfile: redis/redis.Taskfile.yml
    optional: true # Silently ignored if Redis is not installed
  rabbitmq:
    taskfile: rabbitmq/rabbitmq.Taskfile.yml
    optional: true
  postgres:
    taskfile: postgres/postgres.Taskfile.yml
    optional: true
  mysql:
    taskfile: mysql/mysql.Taskfile.yml
    optional: true
  mongodb:
    taskfile: mongodb/mongodb.Taskfile.yml
    optional: true
  monitoring:
    taskfile: monitoring/monitoring.Taskfile.yml
    optional: true
```

---
  </action>
  <verify>
    <automated>
grep -n 'path: redis/redis.compose.yml\|path: &lt;slug&gt;' skills/add-service/SKILL.md | head -5
grep -n 'redis/redis\.Taskfile\.yml' taskfile-templates/root/Taskfile.yml
grep -n '\./redis\.Taskfile\.yml' taskfile-templates/root/Taskfile.yml
grep -n 'D-08\|Root file locations' skills/add-service/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - In `skills/add-service/SKILL.md`:
      - `grep 'path:.*<slug>/<filename>' skills/add-service/SKILL.md` returns at least 2 matches
        (first install and subsequent install blocks in Step 11a)
      - `grep '\- \./.*compose\.yml' skills/add-service/SKILL.md` returns 0 matches in the
        Step 11a append context (old flat include format gone)
      - `grep 'redis/redis\.Taskfile\.yml\|subfolder-relative' skills/add-service/SKILL.md`
        returns at least 1 match in Step 11b commentary
      - `grep 'D-08\|Root file locations' skills/add-service/SKILL.md` returns at least 1 match
    - In `taskfile-templates/root/Taskfile.yml`:
      - `grep 'taskfile: redis/redis.Taskfile.yml' taskfile-templates/root/Taskfile.yml`
        returns exactly 1 match
      - `grep 'taskfile: rabbitmq/rabbitmq.Taskfile.yml' taskfile-templates/root/Taskfile.yml`
        returns exactly 1 match
      - `grep 'taskfile: postgres/postgres.Taskfile.yml' taskfile-templates/root/Taskfile.yml`
        returns exactly 1 match
      - `grep 'taskfile: mysql/mysql.Taskfile.yml' taskfile-templates/root/Taskfile.yml`
        returns exactly 1 match
      - `grep 'taskfile: mongodb/mongodb.Taskfile.yml' taskfile-templates/root/Taskfile.yml`
        returns exactly 1 match
      - `grep 'taskfile: monitoring/monitoring.Taskfile.yml' taskfile-templates/root/Taskfile.yml`
        returns exactly 1 match
      - `grep '\./redis\.Taskfile\.yml' taskfile-templates/root/Taskfile.yml` returns 0 matches
        (old flat path gone)
  </acceptance_criteria>
  <done>
    Step 11a compose.yml include entries use `redis/redis.compose.yml` subfolder paths.
    Step 11b commentary updated to mention subfolder paths and D-08 root file locations.
    taskfile-templates/root/Taskfile.yml updated with subfolder paths for all 6 services.
    No old `./redis.Taskfile.yml` style flat paths remain in the template.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md Step 10 → .devtools/${SERVICE_SLUG}/.env | Credentials written to per-service file; root .env must not receive them |
| taskfile-templates/root/Taskfile.yml → new installs | Template is fetched at runtime; updated paths affect all future installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | .env split | mitigate | Step 10b .env.example documents structure without leaking credentials; per-service .env files are covered by **/.env gitignore from Plan 01 |
| T-04-02-02 | Tampering | root Taskfile template | accept | Template is read-only source in skill repo; only consumed by new installs; existing .devtools/Taskfile.yml not overwritten |
</threat_model>

<verification>
After both tasks complete, verify the changes holistically:

```bash
# Step 11a uses subfolder include paths
grep -c 'path:.*compose\.yml' skills/add-service/SKILL.md
# → must be ≥2 (first install + subsequent install)

# Old flat include format gone from Step 11a
grep -c '\- \./.*\.compose\.yml' skills/add-service/SKILL.md
# → ideally 0, or only in contexts other than Step 11a

# Per-service .env path in Step 10a
grep -c '\.devtools/\${SERVICE_SLUG}/\.env' skills/add-service/SKILL.md
# → must be ≥2

# Root Taskfile template uses subfolder paths
grep -c 'redis/redis\.Taskfile\.yml' taskfile-templates/root/Taskfile.yml
# → must be 1

# Old flat paths gone from template
grep -c '\./redis\.Taskfile\.yml' taskfile-templates/root/Taskfile.yml
# → must be 0
```
</verification>

<success_criteria>
- Step 10a writes service credentials to `.devtools/${SERVICE_SLUG}/.env` (per-service subfolder)
- Step 10a-root exists: writes COMPOSE_PROJECT_NAME + COMPOSE_PROFILES to root `.devtools/.env` on first install only
- Step 10b .env.example documents project-level structure with subfolder layout explanation
- Step 10c monitoring vars go to `.devtools/monitoring/.env`
- Step 10d monitoring files go to `.devtools/monitoring/` subfolder (mkdir included)
- Step 11a `compose.yml` include paths use `redis/redis.compose.yml` format (no leading `./`)
- Step 11b commentary references subfolder paths and D-08 root file locations note
- `taskfile-templates/root/Taskfile.yml` uses `redis/redis.Taskfile.yml` for all 6 services
- No flat paths (`./redis.Taskfile.yml`) remain in the Taskfile template
</success_criteria>

<output>
After completion, create `.planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-02-SUMMARY.md`
</output>
