---
phase: 04-subfolder-output-structure-per-service-subdirectory-layout-i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/add-service/SKILL.md
autonomous: true
requirements:
  - SKILL-03
  - MERGE-01
  - MERGE-04
  - TMPL-08

must_haves:
  truths:
    - "SKILL.md frontmatter description mentions .devtools/<service>/ subfolder layout"
    - "Step 1 detection checks .devtools/${SERVICE}/${SERVICE}.compose.yml (subfolder path)"
    - "Step 1a alias already-installed check uses .devtools/${SERVICE_SLUG}/${SERVICE_SLUG}.compose.yml"
    - "Step 2 .gitignore write includes both .env and **/.env entries"
    - "Step 8 confirmation summary shows subfolder file paths (e.g. .devtools/redis/redis.compose.yml)"
    - "Step 9a adds mkdir -p .devtools/${SERVICE_SLUG}/ before writing compose file to subfolder"
    - "Step 9b writes Taskfile to .devtools/${SERVICE_SLUG}/<filename>.Taskfile.yml"
  artifacts:
    - path: "skills/add-service/SKILL.md"
      provides: "Updated skill with subfolder detection, mkdir, and write paths for Steps 1/1a/2/8/9"
      contains: "test -f .devtools/${SERVICE}/${SERVICE}.compose.yml"
  key_links:
    - from: "Step 1 detection"
      to: "Step 1a rename/alias flow"
      via: "test -f .devtools/${SERVICE}/${SERVICE}.compose.yml"
      pattern: "test -f .devtools/\\$\\{SERVICE\\}/\\$\\{SERVICE\\}\\.compose\\.yml"
    - from: "Step 9a mkdir"
      to: "compose file write"
      via: "mkdir -p .devtools/${SERVICE_SLUG}"
      pattern: "mkdir -p .devtools/\\$\\{SERVICE_SLUG\\}"
---

<objective>
Update SKILL.md Steps 1, 1a, 2, 8, and 9 for the new per-service subfolder output layout.

Purpose: All detection and write logic must use the new `.devtools/<slug>/` subfolder paths
instead of flat `.devtools/` root paths. This is the foundation wave — Plans 02 and 03
depend on these changes being in place first.

Output: Updated `skills/add-service/SKILL.md` with:
- Frontmatter description and `<objective>` referencing the subfolder layout (D-14)
- Step 1 using `test -f .devtools/${SERVICE}/${SERVICE}.compose.yml` (D-11)
- Step 1a alias check using `test -f .devtools/${SERVICE_SLUG}/${SERVICE_SLUG}.compose.yml` (D-11)
- Step 2 `.gitignore` writing both `.env` and `**/.env` (D-12, D-13)
- Step 8 summary showing `.devtools/redis/redis.compose.yml` style paths (D-01–D-04)
- Step 9a with `mkdir -p .devtools/${SERVICE_SLUG}/` before compose write (D-01–D-04)
- Step 9b writing Taskfile to `.devtools/${SERVICE_SLUG}/<filename>.Taskfile.yml` (D-01–D-04)
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-CONTEXT.md
@skills/add-service/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontmatter, objective, Step 1 detection, Step 1a alias check, Step 2 .gitignore</name>
  <files>skills/add-service/SKILL.md</files>
  <read_first>
    - skills/add-service/SKILL.md — read completely before touching anything; all changes are
      find-and-replace operations on exact strings from the current file
    - .planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-CONTEXT.md
      — D-11 (detection paths), D-12 (gitignore), D-13 (no migration note), D-14 (description update)
  </read_first>
  <action>
Apply the following 6 precise find-and-replace edits to `skills/add-service/SKILL.md`.
Do NOT change any other text. Each edit is shown as OLD → NEW.

---

**Edit 1 — Frontmatter description (line 2) (D-14):**

Find EXACTLY:
```
description: "Add a Docker dev service to this project. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB. Writes Docker Compose and Taskfile configs to .devtools/."
```

Replace with:
```
description: "Add a Docker dev service to this project. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB. Writes Docker Compose and Taskfile configs to .devtools/<service>/ (one subfolder per service instance)."
```

---

**Edit 2 — `<objective>` block (D-14):**

Find EXACTLY (the complete objective block, lines 8–16):
```
<objective>
Add a Docker development service to the current project by writing configuration files to
`.devtools/`. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB.

The skill asks for port, image version, and credentials before writing anything. A
configuration summary is shown for confirmation before any file is created. The skill is
safe to re-run: it detects existing installs and either exits cleanly or installs a
named second instance (alias). All credentials land in `.devtools/.env` which is gitignored.
</objective>
```

Replace with:
```
<objective>
Add a Docker development service to the current project by writing configuration files to
`.devtools/<service>/`. Supported services: Redis, RabbitMQ, PostgreSQL, MySQL/MariaDB, MongoDB.

Each service gets its own subdirectory under `.devtools/` containing its compose fragment,
Taskfile, and per-service `.env` (credentials for that service only). Root `.devtools/.env`
holds only project-level vars (`COMPOSE_PROJECT_NAME`, `COMPOSE_PROFILES`).

The skill asks for port, image version, and credentials before writing anything. A
configuration summary is shown for confirmation before any file is created. The skill is
safe to re-run: it detects existing installs and either renames the existing instance (to free
the slot), installs an aliased second instance, or exits cleanly. All credentials land in
`.devtools/<service>/.env` which is gitignored via `**/.env` in `.devtools/.gitignore`.
</objective>
```

---

**Edit 3 — Step 1 detection path (D-11):**

Find EXACTLY (inside the Step 1 bash block):
```
Check whether the service is already installed:
```bash
test -f .devtools/${SERVICE}.compose.yml
```
- If the file **does not exist** → continue to **Step 2**.
- If the file **exists** → go to **Step 1a**.
```

Replace with:
```
Check whether the service is already installed:
```bash
test -f .devtools/${SERVICE}/${SERVICE}.compose.yml
```
- If the file **does not exist** → continue to **Step 2**.
- If the file **exists** → go to **Step 1a**.
```

---

**Edit 4 — Step 1a installed message (D-11, D-14):**

Find EXACTLY:
```
> "`${SERVICE}` is already installed in `.devtools/`."
```

Replace with:
```
> "`${SERVICE}` is already installed at `.devtools/${SERVICE}/`."
```

---

**Edit 5 — Step 1a alias already-installed check (D-11):**

Find EXACTLY (inside the Step 1a bash block):
```
  - Check if alias already installed:
    ```bash
    test -f .devtools/${SERVICE_SLUG}.compose.yml
    ```
  - If file **exists** → output `"${SERVICE_SLUG} is already installed — nothing to do."` and stop. (MERGE-04)
```

Replace with:
```
  - Check if alias already installed:
    ```bash
    test -f .devtools/${SERVICE_SLUG}/${SERVICE_SLUG}.compose.yml
    ```
  - If file **exists** → output `"${SERVICE_SLUG} is already installed — nothing to do."` and stop. (MERGE-04)
```

---

**Edit 6 — Step 2 .gitignore content (D-12, D-13):**

Find EXACTLY:
```
  3. Write `.devtools/.gitignore` with exactly this content (do not add any other entries):
     ```
     .env
     ```
```

Replace with:
```
  3. Write `.devtools/.gitignore` with exactly this content (do not add any other entries):
     ```
     .env
     **/.env
     ```
     This covers both the root `.devtools/.env` and all per-service `.devtools/<slug>/.env` files.
     Do NOT attempt to migrate any existing flat `.devtools/*.compose.yml` files — new installs
     always use the subfolder layout; old flat files continue to work as-is. (D-13: no migration)
```

---
  </action>
  <verify>
    <automated>
grep -n 'devtools/&lt;service&gt;/' skills/add-service/SKILL.md | head -5
grep -n 'test -f .devtools/\${SERVICE}/\${SERVICE}.compose.yml' skills/add-service/SKILL.md
grep -n 'test -f .devtools/\${SERVICE_SLUG}/\${SERVICE_SLUG}.compose.yml' skills/add-service/SKILL.md
grep -n '\*\*/\.env' skills/add-service/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep 'devtools/<service>/' skills/add-service/SKILL.md` returns at least 2 matches
      (frontmatter description and objective block)
    - `grep 'test -f .devtools/\${SERVICE}/\${SERVICE}.compose.yml' skills/add-service/SKILL.md`
      returns exactly 1 match (Step 1)
    - `grep 'test -f .devtools/\${SERVICE_SLUG}/\${SERVICE_SLUG}.compose.yml' skills/add-service/SKILL.md`
      returns exactly 1 match (Step 1a)
    - `grep '\*\*/\.env' skills/add-service/SKILL.md` returns at least 1 match (Step 2 .gitignore)
    - The string `test -f .devtools/${SERVICE}.compose.yml` (flat path, no subfolder) does NOT
      appear anywhere in the file: `grep 'test -f .devtools/\${SERVICE}\.compose\.yml' skills/add-service/SKILL.md`
      must return 0 matches
    - The string `test -f .devtools/${SERVICE_SLUG}.compose.yml` (flat alias path) does NOT appear:
      `grep 'test -f .devtools/\${SERVICE_SLUG}\.compose\.yml' skills/add-service/SKILL.md`
      must return 0 matches
  </acceptance_criteria>
  <done>
    Step 1, Step 1a detection paths, Step 2 .gitignore content, frontmatter description, and
    objective block all updated. Old flat-path detection strings gone from the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Step 8 confirmation summary and Step 9a/9b write paths with mkdir</name>
  <files>skills/add-service/SKILL.md</files>
  <read_first>
    - skills/add-service/SKILL.md — read the current Step 8 (confirmation summary) and
      Step 9 (write per-service files) sections before editing; both are in the `<process>`
      block, Steps 8 and 9 respectively
    - .planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-CONTEXT.md
      — D-01 (subfolder named after slug), D-02 (same slug convention), D-03 (three files per
      subfolder), D-04 (keep <slug>.compose.yml naming), D-07 (no template changes needed)
  </read_first>
  <action>
Apply the following 3 precise find-and-replace edits to `skills/add-service/SKILL.md`.
Do NOT change any other text outside these exact blocks.

---

**Edit 7 — Step 8 files-to-write list (D-01–D-04, D-06):**

Find EXACTLY (the "Files to write:" block in Step 8):
```
Files to write:
  .devtools/.gitignore              (first install only)
  .devtools/redis.compose.yml
  .devtools/redis.Taskfile.yml      (if Taskfile=yes)
  .devtools/compose.yml             (created or appended)
  .devtools/Taskfile.yml            (created on first install only)
  .devtools/.env                    (appended)
  .devtools/.env.example            (appended)
```

Replace with:
```
Files to write:
  .devtools/.gitignore                     (first install only)
  .devtools/redis/redis.compose.yml        (or .devtools/redis-cache/redis-cache.compose.yml for alias)
  .devtools/redis/redis.Taskfile.yml       (if Taskfile=yes)
  .devtools/redis/.env                     (service credentials only)
  .devtools/compose.yml                    (created or appended)
  .devtools/Taskfile.yml                   (created on first install only)
  .devtools/.env                           (COMPOSE_PROJECT_NAME + COMPOSE_PROFILES, first install only)
  .devtools/.env.example                   (updated, first install only)
```

---

**Edit 8 — Step 9a: add mkdir and update write destination (D-01–D-04, D-07):**

Find EXACTLY (the write destination block at the end of Step 9a):
```
Write to the target project:
```
.devtools/<filename>.compose.yml
```
Where `<filename>` is `${SERVICE}` for standard, or `${SERVICE_SLUG}` for alias
(e.g. `redis-cache`).
```

Replace with:
```
First, create the service subdirectory:
```bash
mkdir -p .devtools/${SERVICE_SLUG}
```
Where `${SERVICE_SLUG}` equals `${SERVICE}` for standard installs (e.g. `redis`) and
`${SERVICE}-${ALIAS}` for alias installs (e.g. `redis-cache`). (D-01/D-02)

Write to the target project:
```
.devtools/${SERVICE_SLUG}/<filename>.compose.yml
```
Where `<filename>` is `${SERVICE}` for standard, or `${SERVICE_SLUG}` for alias
(e.g. `.devtools/redis-cache/redis-cache.compose.yml`). (D-03/D-04)

**Note on `env_file: .env`:** The compose template contains `env_file: .env` — Docker Compose
`include:` resolves `env_file` paths relative to the included file's directory. Since the
compose file lives in `.devtools/${SERVICE_SLUG}/`, `env_file: .env` automatically resolves
to `.devtools/${SERVICE_SLUG}/.env`. No template changes are required. (D-07)
```

---

**Edit 9 — Step 9b: update write destination (D-01–D-04):**

Find EXACTLY (the write destination block at the end of Step 9b):
```
Write to the target project:
```
.devtools/<filename>.Taskfile.yml
```
Where `<filename>` is `${SERVICE}` for standard, or `${SERVICE_SLUG}` for alias.
```

Replace with:
```
Write to the target project (subfolder already created by Step 9a mkdir):
```
.devtools/${SERVICE_SLUG}/<filename>.Taskfile.yml
```
Where `<filename>` is `${SERVICE}` for standard, or `${SERVICE_SLUG}` for alias
(e.g. `.devtools/redis-cache/redis-cache.Taskfile.yml`). (D-03/D-04)
```

---
  </action>
  <verify>
    <automated>
grep -n 'mkdir -p .devtools/\${SERVICE_SLUG}' skills/add-service/SKILL.md
grep -n 'devtools/redis/redis.compose.yml' skills/add-service/SKILL.md
grep -n 'devtools/\${SERVICE_SLUG}/\${SERVICE_SLUG}' skills/add-service/SKILL.md
grep -n 'env_file.*resolves' skills/add-service/SKILL.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep 'mkdir -p .devtools/\${SERVICE_SLUG}' skills/add-service/SKILL.md` returns
      at least 1 match (Step 9a)
    - `grep '.devtools/redis/redis.compose.yml' skills/add-service/SKILL.md` returns at least
      1 match (Step 8 summary)
    - `grep '.devtools/redis/redis.Taskfile.yml' skills/add-service/SKILL.md` returns at least
      1 match (Step 8 summary)
    - `grep '.devtools/redis/.env' skills/add-service/SKILL.md` returns at least 1 match
      (Step 8 summary)
    - `grep 'D-07' skills/add-service/SKILL.md` returns at least 1 match (env_file explanation)
    - The string `.devtools/<filename>.compose.yml` (old flat write path) does NOT appear in
      the write destination context: the "Write to the target project:" block in Step 9a must
      now reference `${SERVICE_SLUG}` not a bare `<filename>`:
      `grep '\.devtools/<filename>\.compose\.yml' skills/add-service/SKILL.md` returns 0 matches
    - The string `.devtools/<filename>.Taskfile.yml` (old flat Taskfile path) does NOT appear:
      `grep '\.devtools/<filename>\.Taskfile\.yml' skills/add-service/SKILL.md` returns 0 matches
  </acceptance_criteria>
  <done>
    Step 8 summary shows subfolder paths. Step 9a has mkdir and writes to subfolder.
    Step 9b writes Taskfile to subfolder. D-07 env_file explanation is present.
    No old flat write paths remain in Steps 9a or 9b.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → executor agent | Plan text is the only input; vague instructions produce wrong changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | SKILL.md edit | mitigate | Plan specifies exact find/replace strings; acceptance_criteria verify old strings are gone |
| T-04-01-02 | Repudiation | grep verification | accept | All acceptance_criteria use grep commands — executor can re-run to confirm |
</threat_model>

<verification>
After both tasks complete, verify the SKILL.md changes holistically:

```bash
# Detection paths use subfolder
grep -c 'test -f .devtools/\${SERVICE}/\${SERVICE}.compose.yml' skills/add-service/SKILL.md
# → must be 1

# No old flat detection paths remain
grep -c 'test -f .devtools/\${SERVICE}\.compose\.yml' skills/add-service/SKILL.md
# → must be 0

# mkdir present before compose write
grep -c 'mkdir -p .devtools/\${SERVICE_SLUG}' skills/add-service/SKILL.md
# → must be ≥1

# gitignore has both entries
grep -A1 '\*\*/\.env' skills/add-service/SKILL.md | head -5
# → must show the context around **/.env being present

# Subfolder paths in Step 8 summary
grep -c 'devtools/redis/redis\.compose\.yml' skills/add-service/SKILL.md
# → must be ≥1
```
</verification>

<success_criteria>
- Frontmatter `description` mentions `.devtools/<service>/` subfolder layout
- `<objective>` block references per-service subfolder, per-service `.env`, and minimal root `.env`
- Step 1 uses `test -f .devtools/${SERVICE}/${SERVICE}.compose.yml`
- Step 1a uses `test -f .devtools/${SERVICE_SLUG}/${SERVICE_SLUG}.compose.yml`
- Step 2 `.gitignore` block contains both `.env` and `**/.env`
- Step 8 files-to-write list shows `.devtools/redis/redis.compose.yml` style paths
- Step 9a has `mkdir -p .devtools/${SERVICE_SLUG}` before the write destination
- Step 9b write destination shows `.devtools/${SERVICE_SLUG}/<filename>.Taskfile.yml`
- `env_file: .env` Docker Compose resolution explained in Step 9a (D-07)
- No old flat-path strings (`.devtools/${SERVICE}.compose.yml`, `.devtools/<filename>.compose.yml`) remain in Steps 1, 1a, 9a, 9b
</success_criteria>

<output>
After completion, create `.planning/phases/04-subfolder-output-structure-per-service-subdirectory-layout-i/04-01-SUMMARY.md`
</output>
